<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".activity.MainActivity"
    tools:showIn="@layout/app_bar_main">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageButton
            android:layout_width="fill_parent"
            android:layout_height="50dp"
            android:id="@+id/gotoListBtn"
            android:layout_margin="15dp"
            android:background="@color/button_material_light"
            android:foreground="@drawable/ic_menu_barcode"
            android:visibility="visible"
            android:src="@drawable/ic_menu_list" />

        <ImageButton
            android:layout_width="fill_parent"
            android:layout_height="50dp"
            android:id="@+id/barcodeScanBtn"
            android:layout_margin="15dp"
            android:background="@color/button_material_light"
            android:foreground="@drawable/ic_menu_barcode"
            android:visibility="visible"
            android:src="@drawable/ic_menu_barcode" />

        <ImageButton
            android:layout_width="fill_parent"
            android:layout_height="50dp"
            android:id="@+id/imageScanBtn"
            android:layout_margin="15dp"
            android:background="@color/button_material_light"
            android:foregroundTint="#160e8a"
            android:foreground="@android:drawable/ic_dialog_dialer"
            android:visibility="visible"
            android:src="@drawable/ic_menu_camera" />

        <LinearLayout
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/scannedImageView" />
        </LinearLayout>

    </LinearLayout>

</RelativeLayout>
